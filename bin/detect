#!/usr/bin/env bash
# bin/detect <build-dir>

source "$(dirname "$( realpath "$0" )")/utils.sh"


if [ -z "${WAR_PATH}" ]; then
    java::war::find_war
fi

if [ -n "${WAR_PATH}" ] && [ -f "${HOME}/${WAR_PATH}" ]; then
    echo "WAR"
    exit 0
fi

echo "no"
exit 1
